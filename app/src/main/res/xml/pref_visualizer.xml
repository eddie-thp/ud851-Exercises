<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TODO (2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="@bool/pref_bass_default"
        android:key="@string/pref_bass_key"
        android:summaryOff="@string/pref_bass_hidden"
        android:summaryOn="@string/pref_bass_shown"
        android:title="@string/pref_bass_label" />

</PreferenceScreen>
